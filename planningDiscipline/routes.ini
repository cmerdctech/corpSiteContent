

routes._default.route = ""
routes._default.defaults.title='The Planning Discipline: Welcome'
routes._default.defaults.module = scrollFeature		;page type
routes._default.defaults.controller = generate		;always the same
routes._default.defaults.action =container			;always the same


routes.template.route = "template"
routes.template.defaults.title='The Planning Discipline: Welcome'
routes.template.defaults.module = scrollFeature		;page type
routes.template.defaults.controller = generate		;always the same
routes.template.defaults.action =container			;always the same




routes.annualRollover.route = "annualRollover"
routes.annualRollover.defaults.title='The Planning Discipline: Welcome'
routes.annualRollover.defaults.module = scrollFeature		;page type
routes.annualRollover.defaults.controller = generate		;always the same
routes.annualRollover.defaults.action =container			;always the same





routes.tmp1.route = "tmp1"
routes.tmp1.defaults.title='The Planning Discipline: Welcome'
routes.tmp1.defaults.module = nonFrameworkPureHtml		;page type
routes.tmp1.defaults.controller = generate		;always the same
routes.tmp1.defaults.action =container			;always the same



routes.tmp2.route = "tmp2"
routes.tmp2.defaults.title='The Planning Discipline: Welcome'
routes.tmp2.defaults.module = pureHtml		;page type
routes.tmp2.defaults.controller = generate		;always the same
routes.tmp2.defaults.action =container			;always the same



routes.tmp3.route = "tmp3"
routes.tmp3.defaults.title='The Planning Discipline: Welcome'
routes.tmp3.defaults.module = pureHtml		;page type
routes.tmp3.defaults.controller = generate		;always the same
routes.tmp3.defaults.action =container			;always the same



routes.sitemap.route = '/sitemap/'
routes.sitemap.noList = true						;noList==true prevents route from appearing on sitemap
routes.sitemap.defaults.module = siteMap		;page type
routes.sitemap.defaults.controller = generate		;always the same
routes.sitemap.defaults.action =container			;always the same